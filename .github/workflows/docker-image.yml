name: Deploy backend

on:
  push:
    branches: [ "main" ]
    
env:
  POSTGRES_USER: root
  POSTGRES_PASSWORD: root

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: docker-compose
        run: docker-compose up -d
